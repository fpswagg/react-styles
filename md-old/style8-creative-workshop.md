# Proposition 8 — **Creative Workshop** (Design workshop créatif — artisanal & organique)

## 1. Résumé

Atelier créatif virtuel où l'inspiration rencontre l'artisanat digital. L'interface présente un environnement organique avec des éléments artisanaux, des textures naturelles et des animations ludiques. Le dashboard organise les projets créatifs tandis que le formulaire guide l'utilisateur dans un processus de création intuitif et inspirant.

## 2. Objectifs

* Créer un environnement stimulant pour la créativité
* Faciliter l'expression artistique et l'innovation
* Maintenir l'inspiration tout au long du processus créatif
* Favoriser l'expérimentation et l'itération rapide

## 3. Personas

* **Créateur Artistique** : Designer, illustrateur, artiste numérique
* **Artisan Digital** : Développeur créatif, motion designer
* **Directeur Artistique** : Supervise et inspire les projets créatifs
* **Étudiant en Arts** : Apprend et expérimente dans un environnement stimulant

## 4. Flux utilisateur (wireframe organique)

```
╭─────────────────────────────────────────────────────────────╮
│                    🎨 CREATIVE WORKSHOP                       │
│                                                             │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐     │
│  │💡   │ │🎭   │ │🎨   │ │📐   │ │🎪   │ │🌟   │ │✨   │     │
│  │ IDE │ │ SKETCH│ │ART  │ │TOOL │ │PROJ │ │INSP │ │MAGIC│     │
│  │ AS  │ │ PAD  │ │WORK │ │BOX  │ │ECTS │ │IRE  │ │WAND │     │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘     │
│                                                             │
│ ╭─────────────────────────────────────────────────────────╮ │
│ │                    PROJET EN COURS                        │ │
│ │                                                         │ │
│ │ 🎭 "Rêverie Automnale" - Illustration botanique          │ │
│ │ ├─ Phase: 🌱 Croquis préparatoires                      │ │
│ │ ├─ Inspirations: 🍂 feuilles dorées, 🌧️ pluie fine      │ │
│ │ ├─ Outils: ✏️ crayons numériques, 🎨 aquarelle digitale   │ │
│ │ ├─ Deadline: Dans 2 semaines                           │ │
│ │ └─ Humeur créative: ✨ Inspiré!                         │ │
│ ╰─────────────────────────────────────────────────────────╯ │
│                                                             │
│ [🎭 SKETCHBOOK] [🎨 PALETTE] [📐 OUTILS] [🌟 INSPIRATION]    │
╰─────────────────────────────────────────────────────────────╯
```

## 5. Formulaire (processus créatif) — structure organique

### **Étape 1: L'Inspiration**
```
┌─────────────────────────────────────────────────────────────┐
│ 🌟 ÉTAPE 1 - L'INSPI RATION                                 │
│                                                             │
│ Quel est votre mood créatif actuel ?                        │
│ ◉ 🌅 Énergique & dynamique                                 │
│ ◉ 🌙 Calme & introspectif                                  │
│ ◉ 🌈 Ludique & coloré                                       │
│ ◉ 🌿 Naturel & organique                                   │
│ ◉ ✨ Magique & fantastique                                 │
│                                                             │
│ Mots-clés d'inspiration :                                   │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Automne, feuilles, pluie, introspection, mélancolie     │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ Images d'inspiration :                                      │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 📎 forêt_automne.jpg (2.1 MB) ✓                         │ │
│ │ 📎 esquisse_main.jpg (856 KB) ✓                         │ │
│ │ 📎 palette_couleurs.png (445 KB) ✓                      │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ [🌱 GERMER L'IDÉE]                                         │
└─────────────────────────────────────────────────────────────┘
```

### **Étape 2: L'Exploration**
```
┌─────────────────────────────────────────────────────────────┐
│ 🎨 ÉTAPE 2 - L'EXPLORATION                                  │
│                                                             │
│ Techniques à explorer :                                     │
│ ☑️ Aquarelle digitale                                       │
│ ☑️ Calligraphie                                             │
│ ◉ Gravure numérique                                        │
│ ◉ Motion design                                            │
│                                                             │
│ Style visuel souhaité :                                     │
│ ◉ Minimaliste & épuré                                      │
│ ◉ Ornemental & détaillé                                    │
│ ◉ Organique & naturel                                      │
│ ◉ Géométrique & structuré                                  │
│                                                             │
│ Palette de couleurs :                                       │
│ 🎨 [Rouge brique] [Orange brûlé] [Jaune miel] [Beige sable] │
│                                                             │
│ [🎭 COMMENCER À CRÉER]                                      │
└─────────────────────────────────────────────────────────────┘
```

### **Étape 3: La Création**
```
┌─────────────────────────────────────────────────────────────┐
│ 🎭 ÉTAPE 3 - LA CRÉATION                                    │
│                                                             │
│ Outils nécessaires :                                        │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🖌️ Pinceaux aquarelle • ✏️ Crayons numériques            │ │
│ │ 🎨 Palette mélangée • 📐 Règles de composition           │ │
│ │ ✨ Effets de texture • 🌊 Dégradés organiques             │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ Zone de création libre :                                    │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🎨 CANVAS INTERACTIF - Dessinez ici !                   │ │
│ │                                                         │ │
│ │ [🖌️] [✏️] [🎨] [📐] [✨] [🌊]                           │ │
│ │                                                         │ │
│ │ 💡 Astuce : Laissez votre intuition guider votre trait   │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ [🌟 FINALISER LE PROJET]                                    │
└─────────────────────────────────────────────────────────────┘
```

## 6. Dashboard — éléments organiques

### **Widget Atelier Créatif**
```javascript
// Composant React pour l'atelier créatif
const CreativeWorkshop = () => {
  const [projects, setProjects] = useState([
    {
      id: 1,
      title: "Rêverie Automnale",
      phase: "sketch",
      mood: "inspiré",
      tools: ["aquarelle", "crayons"],
      inspiration: ["🍂 feuilles", "🌧️ pluie"],
      deadline: "2024-11-15",
      progress: 35
    }
  ]);

  const moodEmojis = {
    "inspiré": "✨",
    "bloqué": "😵",
    "euphorique": "🎉",
    "concentré": "🎯",
    "rêveur": "💭"
  };

  return (
    <div className="creative-workshop">
      <div className="inspiration-wall">
        <h2>✨ Atelier Créatif</h2>

        {projects.map(project => (
          <motion.div
            key={project.id}
            className="project-organism"
            initial={{ scale: 0, rotate: -10 }}
            animate={{ scale: 1, rotate: 0 }}
            whileHover={{ scale: 1.05, rotate: 2 }}
            transition={{
              type: "spring",
              stiffness: 300,
              damping: 20,
              hover: { duration: 0.3 }
            }}
          >
            <div className="project-header">
              <h3>{project.title}</h3>
              <span className="mood-indicator">
                {moodEmojis[project.mood]} {project.mood}
              </span>
            </div>

            <div className="creative-phase">
              <div className="phase-indicator">
                <span className="phase-emoji">
                  {project.phase === 'sketch' ? '🎭' :
                   project.phase === 'creation' ? '🎨' : '✨'}
                </span>
                <span className="phase-text">
                  {project.phase === 'sketch' ? 'Phase d\'esquisse' :
                   project.phase === 'creation' ? 'Création active' : 'Finalisation'}
                </span>
              </div>
            </div>

            <div className="inspiration-tags">
              {project.inspiration.map(inspo => (
                <span key={inspo} className="inspo-tag">
                  {inspo}
                </span>
              ))}
            </div>

            <div className="creative-tools">
              <h4>Outils utilisés :</h4>
              <div className="tool-icons">
                {project.tools.map(tool => (
                  <span key={tool} className="tool-icon" title={tool}>
                    {tool === 'aquarelle' ? '🎨' :
                     tool === 'crayons' ? '✏️' : '🖌️'}
                  </span>
                ))}
              </div>
            </div>

            <div className="progress-organism">
              <div className="progress-label">Progression créative</div>
              <div className="progress-bar">
                <motion.div
                  className="progress-fill"
                  initial={{ width: 0 }}
                  animate={{ width: `${project.progress}%` }}
                  transition={{ duration: 1.5, ease: "easeOut" }}
                />
              </div>
              <span className="progress-text">{project.progress}%</span>
            </div>
          </motion.div>
        ))}
      </div>
    </div>
  );
};
```

### **Widget Palette d'Inspiration**
```
┌─────────────────────────────────────────────────────────────┐
│ 🎨 PALETTE D'INSPIRATION                                   │
│                                                             │
│ Couleurs de saison :                                        │
│ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐            │
│ │🍂   │ │🌰   │ │🟫   │ │🟤   │ │🟡   │ │🔶   │            │
│ │Rouge│ │Marron│ │Beige│ │Orange│ │Jaune│ │Doré │            │
│ │autom│ │nois. │ │sable│ │brûlé│ │miel │ │     │            │
│ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘            │
│                                                             │
│ Textures disponibles :                                      │
│ ◉ Papier froissé ◉ Toile de lin ◉ Bois vieilli             │
│ ◉ Métal oxydé ◉ Pierre naturelle ◉ Verre dépoli            │
│                                                             │
│ Mots inspirants :                                           │
│ 🌿 organique • 🎭 poétique • ✨ éthéré • 🌊 fluide          │
│                                                             │
│ [🔄 Générer nouvelle palette]                               │
└─────────────────────────────────────────────────────────────┘
```

## 7. Schéma de données (MongoDB créatif)

```javascript
// Schéma MongoDB pour atelier créatif
const mongoose = require('mongoose');

const CreativeProjectSchema = new mongoose.Schema({
  _id: { type: mongoose.Schema.Types.ObjectId, auto: true },

  // Informations de base
  title: { type: String, required: true },
  creator: {
    userId: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
    name: { type: String, required: true },
    avatar: String
  },

  // Processus créatif
  creativeProcess: {
    inspiration: {
      mood: {
        type: String,
        enum: ['énergique', 'calme', 'ludique', 'naturel', 'magique'],
        required: true
      },
      keywords: [{ type: String }],
      referenceImages: [{
        filename: String,
        url: String,
        uploadedAt: { type: Date, default: Date.now }
      }]
    },

    exploration: {
      techniques: [{
        type: String,
        enum: ['aquarelle-digitale', 'calligraphie', 'gravure', 'motion-design']
      }],
      style: {
        type: String,
        enum: ['minimaliste', 'ornemental', 'organique', 'géométrique']
      },
      colorPalette: [{
        name: String,
        hex: String,
        emoji: String
      }]
    },

    creation: {
      tools: [{
        name: String,
        category: {
          type: String,
          enum: ['dessin', 'couleur', 'effets', 'composition']
        },
        emoji: String
      }],
      canvas: {
        width: Number,
        height: Number,
        layers: [{
          id: String,
          name: String,
          visible: { type: Boolean, default: true },
          opacity: { type: Number, default: 1 }
        }]
      }
    }
  },

  // Métadonnées créatives
  tags: [{ type: String }],
  category: {
    type: String,
    enum: ['illustration', 'design', 'animation', 'photographie', 'mixed-media']
  },
  mood: {
    type: String,
    enum: ['inspiré', 'bloqué', 'euphorique', 'concentré', 'rêveur']
  },

  // Progression et statut
  status: {
    type: String,
    enum: ['idéation', 'esquisse', 'création', 'finalisation', 'terminé'],
    default: 'idéation'
  },
  progress: { type: Number, min: 0, max: 100, default: 0 },

  // Temporalité
  deadline: Date,
  timeSpent: { type: Number, default: 0 }, // en minutes
  lastActivity: { type: Date, default: Date.now },

  // Collaboration
  collaborators: [{
    userId: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
    role: {
      type: String,
      enum: ['viewer', 'contributor', 'co-creator']
    },
    joinedAt: { type: Date, default: Date.now }
  }],

  // Versions et itérations
  versions: [{
    versionNumber: Number,
    title: String,
    description: String,
    createdAt: { type: Date, default: Date.now },
    files: [{
      filename: String,
      url: String,
      type: String
    }]
  }],

  // Statistiques
  stats: {
    views: { type: Number, default: 0 },
    likes: { type: Number, default: 0 },
    shares: { type: Number, default: 0 },
    comments: { type: Number, default: 0 }
  },

  // Paramètres
  isPublic: { type: Boolean, default: false },
  allowComments: { type: Boolean, default: true },
  allowCollaboration: { type: Boolean, default: false },

  // Timestamps
  createdAt: { type: Date, default: Date.now },
  updatedAt: { type: Date, default: Date.now }
});

// Indexes pour performance
CreativeProjectSchema.index({ creator: 1, createdAt: -1 });
CreativeProjectSchema.index({ tags: 1 });
CreativeProjectSchema.index({ category: 1 });
CreativeProjectSchema.index({ status: 1 });

// Middleware pour updatedAt
CreativeProjectSchema.pre('save', function(next) {
  this.updatedAt = new Date();
  next();
});

module.exports = mongoose.model('CreativeProject', CreativeProjectSchema);
```

## 8. Stack & bibliothèques créatives

### **Frontend créatif**
```javascript
// package.json pour atelier créatif
{
  "name": "creative-workshop",
  "version": "1.0.0",
  "dependencies": {
    "react": "^18.2.0",
    "framer-motion": "^10.12.0",           // Animations organiques
    "react-spring": "^9.7.0",              // Transitions fluides
    "fabric.js": "^6.0.0",                 // Canvas interactif
    "react-colorful": "^5.6.1",            // Sélecteur couleurs créatif
    "react-dropzone": "^14.2.0",           // Upload intuitif
    "react-sketch-canvas": "^1.0.0",       // Sketching libre
    "three.js": "^0.155.0",                // 3D pour effets spéciaux
    "tone.js": "^14.7.77",                 // Audio pour ambiance
    "react-confetti": "^6.1.0",            // Célébrations créatives
    "react-masonry-css": "^1.0.16",        // Layout organique
    "react-spring-carousel": "^2.0.0"      // Carrousels fluides
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.24",
    "tailwindcss": "^3.3.2"
  }
}
```

### **API créative**
```javascript
// routes/creative.js - API pour atelier créatif
const express = require('express');
const router = express.Router();
const CreativeProject = require('../models/CreativeProject');
const multer = require('multer');
const path = require('path');

// Configuration upload
const storage = multer.diskStorage({
  destination: (req, file, cb) => {
    cb(null, 'uploads/creative/');
  },
  filename: (req, file, cb) => {
    const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1E9);
    cb(null, 'creative-' + uniqueSuffix + path.extname(file.originalname));
  }
});

const upload = multer({
  storage: storage,
  limits: { fileSize: 10 * 1024 * 1024 }, // 10MB
  fileFilter: (req, file, cb) => {
    const allowedTypes = /jpeg|jpg|png|gif|svg|pdf|ai|psd/;
    const extname = allowedTypes.test(path.extname(file.originalname).toLowerCase());
    const mimetype = allowedTypes.test(file.mimetype);

    if (mimetype && extname) {
      return cb(null, true);
    } else {
      cb(new Error('Type de fichier non autorisé'));
    }
  }
});

// Créer un nouveau projet créatif
router.post('/projects', upload.array('inspirationFiles', 10), async (req, res) => {
  try {
    const projectData = {
      title: req.body.title,
      creator: {
        userId: req.user._id,
        name: req.user.name,
        avatar: req.user.avatar
      },
      creativeProcess: {
        inspiration: {
          mood: req.body.mood,
          keywords: req.body.keywords ? req.body.keywords.split(',') : [],
          referenceImages: req.files ? req.files.map(file => ({
            filename: file.filename,
            url: `/uploads/creative/${file.filename}`
          })) : []
        }
      },
      tags: req.body.tags ? req.body.tags.split(',') : [],
      category: req.body.category
    };

    const project = new CreativeProject(projectData);
    await project.save();

    res.status(201).json({
      success: true,
      project: project,
      message: '✨ Projet créatif créé avec succès !'
    });

  } catch (error) {
    console.error('Erreur création projet:', error);
    res.status(500).json({
      success: false,
      message: 'Erreur lors de la création du projet'
    });
  }
});

// Obtenir projets avec pagination créative
router.get('/projects', async (req, res) => {
  try {
    const page = parseInt(req.query.page) || 1;
    const limit = parseInt(req.query.limit) || 12;
    const skip = (page - 1) * limit;

    const filter = {};
    if (req.query.category) filter.category = req.query.category;
    if (req.query.mood) filter.mood = req.query.mood;
    if (req.query.tags) filter.tags = { $in: req.query.tags.split(',') };

    const projects = await CreativeProject
      .find(filter)
      .populate('creator.userId', 'name avatar')
      .sort({ createdAt: -1 })
      .skip(skip)
      .limit(limit)
      .lean();

    const total = await CreativeProject.countDocuments(filter);

    res.json({
      projects,
      pagination: {
        current: page,
        total: Math.ceil(total / limit),
        count: projects.length
      }
    });

  } catch (error) {
    res.status(500).json({ message: 'Erreur récupération projets' });
  }
});

// Mettre à jour progression créative
router.patch('/projects/:id/progress', async (req, res) => {
  try {
    const { progress, mood, status } = req.body;

    const updateData = {
      updatedAt: new Date(),
      lastActivity: new Date()
    };

    if (progress !== undefined) updateData.progress = progress;
    if (mood) updateData.mood = mood;
    if (status) updateData.status = status;

    const project = await CreativeProject.findByIdAndUpdate(
      req.params.id,
      updateData,
      { new: true }
    );

    if (!project) {
      return res.status(404).json({ message: 'Projet non trouvé' });
    }

    // Célébration si projet terminé
    if (progress === 100) {
      // Ici on pourrait déclencher des confettis côté client
      project.completedAt = new Date();
      await project.save();
    }

    res.json({
      success: true,
      project,
      celebration: progress === 100
    });

  } catch (error) {
    res.status(500).json({ message: 'Erreur mise à jour progression' });
  }
});

// Générer palette de couleurs inspirée
router.post('/inspiration/palette', async (req, res) => {
  try {
    const { mood, season } = req.body;

    // Algorithme de génération de palette basé sur mood et saison
    const palettes = {
      'énergique-printemps': [
        { name: 'Vert menthe', hex: '#98FB98', emoji: '🌱' },
        { name: 'Jaune citron', hex: '#FFFF00', emoji: '☀️' },
        { name: 'Rose poudré', hex: '#FFB6C1', emoji: '🌸' }
      ],
      'calme-automne': [
        { name: 'Rouge brique', hex: '#CD5C5C', emoji: '🍂' },
        { name: 'Orange brûlé', hex: '#CD853F', emoji: '🎃' },
        { name: 'Beige sable', hex: '#F4A460', emoji: '🏜️' }
      ]
      // ... autres palettes
    };

    const key = `${mood}-${season}`;
    const palette = palettes[key] || palettes['calme-automne'];

    res.json({
      success: true,
      palette,
      mood,
      season
    });

  } catch (error) {
    res.status(500).json({ message: 'Erreur génération palette' });
  }
});

module.exports = router;
```

## 9. Plan d'implémentation créative

### **Phase 1: Canvas Créatif (Semaine 1-2)**
```javascript
// Composant Canvas principal
import { Canvas, useFrame } from '@react-three/fiber';
import { useRef, useState } from 'react';

const CreativeCanvas = ({ project, onSave }) => {
  const meshRef = useRef();
  const [brushColor, setBrushColor] = useState('#8B4513');
  const [brushSize, setBrushSize] = useState(5);

  useFrame((state, delta) => {
    // Animation subtile du canvas
    if (meshRef.current) {
      meshRef.current.rotation.z += delta * 0.1;
    }
  });

  return (
    <div className="creative-canvas-container">
      <Canvas className="creative-canvas">
        <ambientLight intensity={0.5} />
        <pointLight position={[10, 10, 10]} />

        <mesh ref={meshRef}>
          <planeGeometry args={[10, 10]} />
          <meshStandardMaterial color="#FEFEFE" />
        </mesh>
      </Canvas>

      <div className="canvas-tools">
        <div className="color-palette">
          <button onClick={() => setBrushColor('#8B4513')}>🍂</button>
          <button onClick={() => setBrushColor('#D2691E')}>🎃</button>
          <button onClick={() => setBrushColor('#DAA520')}>✨</button>
          <button onClick={() => setBrushColor('#556B2F')}>🌿</button>
        </div>

        <div className="brush-sizes">
          <button onClick={() => setBrushSize(2)}>✏️</button>
          <button onClick={() => setBrushSize(5)}>🖌️</button>
          <button onClick={() => setBrushSize(10)}>🎨</button>
        </div>
      </div>
    </div>
  );
};
```

### **Phase 2: Interface Organique (Semaine 3-5)**
1. **Layout adaptatif**
   - Grille organique avec Masonry
   - Animations d'entrée échelonnées
   - Transitions fluides entre vues

2. **Composants créatifs**
   - Sélecteur de couleurs intuitif
   - Palette d'outils contextuelle
   - Aperçu temps réel

3. **Gestion d'état créative**
   - Historique des actions (undo/redo)
   - Sauvegarde automatique
   - Synchronisation cloud

### **Phase 3: Fonctionnalités avancées (Semaine 6-8)**
1. **Collaboration temps réel**
   - Curseurs multi-utilisateurs
   - Commentaires sur canvas
   - Partage de session

2. **IA créative**
   - Génération de palettes
   - Suggestions de composition
   - Analyse de style

3. **Export et partage**
   - Export multi-formats
   - Galerie publique
   - Intégration réseaux sociaux

## 10. Design System Organique

### **Palette naturelle**
```css
:root {
  /* Couleurs terreuses et naturelles */
  --creative-primary: #8B4513;      /* Marron artisanal */
  --creative-secondary: #FEFEFE;    /* Blanc cassé */
  --creative-accent: #D2691E;       /* Orange artisanal */

  /* Couleurs organiques */
  --creative-earth: #8B4513;        /* Terre */
  --creative-clay: #D2691E;         /* Argile */
  --creative-sand: #F4A460;         /* Sable */
  --creative-leaf: #556B2F;         /* Feuille */
  --creative-sky: #87CEEB;          /* Ciel */
  --creative-sun: #DAA520;          /* Soleil */

  /* Couleurs saisonnières */
  --season-autumn: #CD5C5C;         /* Automne */
  --season-winter: #4682B4;         /* Hiver */
  --season-spring: #98FB98;         /* Printemps */
  --season-summer: #FFFF00;         /* Été */
}
```

### **Animations organiques**
```css
/* Animations avec ressort et naturel */
@keyframes organic-bounce {
  0%, 100% { transform: translateY(0) rotate(0deg); }
  25% { transform: translateY(-10px) rotate(1deg); }
  50% { transform: translateY(-5px) rotate(-0.5deg); }
  75% { transform: translateY(-15px) rotate(0.5deg); }
}

@keyframes fluid-flow {
  0% { transform: translateX(-100%) scaleX(0); }
  50% { transform: translateX(0%) scaleX(1); }
  100% { transform: translateX(100%) scaleX(0); }
}

@keyframes particle-float {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  25% { transform: translateY(-20px) rotate(90deg); }
  50% { transform: translateY(-10px) rotate(180deg); }
  75% { transform: translateY(-30px) rotate(270deg); }
}

/* Classes d'animation */
.organic-hover {
  transition: all 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.organic-hover:hover {
  transform: translateY(-6px) rotate(1deg);
  box-shadow: 0 8px 16px rgba(139, 69, 19, 0.15);
}

.fluid-transition {
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.particle-element {
  animation: particle-float 6s ease-in-out infinite;
}
```

### **Composants artisanaux**
```javascript
// Composant Button artisanal
const OrganicButton = ({
  children,
  variant = 'earth',
  size = 'medium',
  onClick
}) => {
  const [isRippling, setIsRippling] = useState(false);

  const handleClick = (e) => {
    setIsRippling(true);
    setTimeout(() => setIsRippling(false), 600);

    onClick && onClick(e);
  };

  return (
    <motion.button
      className={`organic-button organic-button-${variant} organic-button-${size}`}
      onClick={handleClick}
      whileHover={{ scale: 1.05 }}
      whileTap={{ scale: 0.95 }}
      transition={{ type: "spring", stiffness: 300, damping: 20 }}
    >
      {children}

      {isRippling && (
        <motion.div
          className="ripple-effect"
          initial={{ scale: 0, opacity: 0.6 }}
          animate={{ scale: 4, opacity: 0 }}
          transition={{ duration: 0.6 }}
        />
      )}
    </motion.button>
  );
};

// Utilisation
<OrganicButton variant="clay" size="large" onClick={handleCreate}>
  🎨 Créer un chef-d'œuvre
</OrganicButton>
```

## 10.1. Ambiance & Concept

**Atmosphère** : Atelier d'artiste où la créativité s'exprime librement. L'environnement est organique, chaleureux, invitant à l'expérimentation. Les éléments s'animent naturellement, créant une expérience immersive et inspirante.

**Identité visuelle** : Design artisanal avec des textures naturelles, des couleurs terreuses et des animations organiques. L'interface semble vivante, évoluant avec l'utilisateur.

## 10.2. Arrière-plans & Textures

* **Background principal** : Dégradé naturel terreux avec texture de bois/canvas
* **Pattern overlay** : Particules organiques flottantes, motifs naturels subtils
* **Cards** : Fond texturé avec bordures irrégulières, ombres douces
* **Zones de contenu** : Espacement organique, pas de grilles rigides
* **Texture de surface** : Grain de papier artisanal, variations naturelles

## 10.3. Animations & Transitions

**Micro-interactions** :
* **Cards hover** : Translation Y + rotation légère avec ressort organique
* **Boutons** : Scale + rotation au clic avec rebond naturel
* **Form inputs focus** : Border color change + glow naturel
* **Navigation** : Transitions fluides avec easing organique
* **Loading states** : Particules animées, indicateurs naturels

**Transitions de page** :
* **Navigation** : Fade + rotation subtile avec ressort
* **Modal** : Scale + fade avec animation élastique
* **Form steps** : Transitions fluides avec morphing

**Animations d'état** :
* **Success** : Confettis artisanaux, célébration organique
* **Error** : Shake doux, feedback naturel
* **Loading** : Particules flottantes, animation calme

## 10.4. Idées & Détails Design

**Éléments uniques** :
* **Canvas interactif** : Zone de dessin libre avec outils naturels
* **Palette de couleurs saisonnières** : Génération de palettes par mood
* **Animations de particules** : Éléments flottants organiques
* **Textures procédurales** : Génération de textures naturelles
* **Interface morphing** : Formes qui s'adaptent au contenu

**Interactions spéciales** :
* **Dessin libre** : Canvas avec reconnaissance de gestes
* **Drag organique** : Glisser-déposer avec physique naturelle
* **Double-tap magique** : Actions spéciales avec double-tap
* **Shake to randomize** : Secouer pour générer de nouvelles idées

**Composants signature** :
* **Mood selector** : Sélecteur d'humeur avec emojis et couleurs
* **Creative timeline** : Chronologie des étapes créatives
* **Inspiration wall** : Mur d'inspiration dynamique
* **Tool belt organique** : Barre d'outils qui s'adapte

## 11. Code Examples Détaillés

### **Canvas créatif avec Fabric.js**
```javascript
import { fabric } from 'fabric';
import { useEffect, useRef } from 'react';

const CreativeCanvas = ({ width = 800, height = 600, onSave }) => {
  const canvasRef = useRef(null);
  const fabricCanvasRef = useRef(null);

  useEffect(() => {
    // Initialisation du canvas Fabric
    const canvas = new fabric.Canvas(canvasRef.current, {
      width: width,
      height: height,
      backgroundColor: '#FEFEFE',
      selection: true,
      renderOnAddRemove: true
    });

    fabricCanvasRef.current = canvas;

    // Ajouter des textures de fond
    const texture = new fabric.Rect({
      left: 0,
      top: 0,
      width: width,
      height: height,
      fill: new fabric.Pattern({
        source: '/textures/paper-texture.png',
        repeat: 'repeat'
      }),
      selectable: false,
      evented: false
    });

    canvas.add(texture);
    texture.sendToBack();

    // Gestionnaire de sauvegarde
    const saveInterval = setInterval(() => {
      const data = canvas.toJSON();
      onSave && onSave(data);
    }, 30000); // Sauvegarde auto toutes les 30s

    return () => {
      clearInterval(saveInterval);
      canvas.dispose();
    };
  }, [width, height, onSave]);

  const addBrushStroke = (color = '#8B4513', size = 5) => {
    const canvas = fabricCanvasRef.current;
    if (!canvas) return;

    canvas.freeDrawingBrush.color = color;
    canvas.freeDrawingBrush.width = size;
    canvas.isDrawingMode = true;
  };

  const addShape = (type) => {
    const canvas = fabricCanvasRef.current;
    if (!canvas) return;

    let shape;

    switch (type) {
      case 'circle':
        shape = new fabric.Circle({
          radius: 50,
          fill: '#D2691E',
          left: Math.random() * (width - 100),
          top: Math.random() * (height - 100)
        });
        break;
      case 'rectangle':
        shape = new fabric.Rect({
          left: Math.random() * (width - 100),
          top: Math.random() * (height - 100),
          width: 100,
          height: 80,
          fill: '#F4A460'
        });
        break;
    }

    if (shape) {
      canvas.add(shape);
    }
  };

  return (
    <div className="creative-canvas-wrapper">
      <canvas ref={canvasRef} className="creative-canvas" />

      <div className="canvas-toolbar">
        <div className="brush-tools">
          <button onClick={() => addBrushStroke('#8B4513', 2)}>✏️</button>
          <button onClick={() => addBrushStroke('#D2691E', 5)}>🖌️</button>
          <button onClick={() => addBrushStroke('#DAA520', 10)}>🎨</button>
        </div>

        <div className="shape-tools">
          <button onClick={() => addShape('circle')}>⭕</button>
          <button onClick={() => addShape('rectangle')}>▭</button>
        </div>

        <div className="action-tools">
          <button onClick={() => fabricCanvasRef.current?.clear()}>
            🗑️
          </button>
          <button onClick={() => {
            const dataURL = fabricCanvasRef.current?.toDataURL();
            const link = document.createElement('a');
            link.download = 'creation-artisanale.png';
            link.href = dataURL;
            link.click();
          }}>
            💾
          </button>
        </div>
      </div>
    </div>
  );
};
```

### **Hook pour génération de palettes**
```javascript
import { useState, useCallback } from 'react';

const useCreativePalette = () => {
  const [currentPalette, setCurrentPalette] = useState([]);

  // Palettes par mood et saison
  const paletteDatabase = {
    'énergique-printemps': [
      { name: 'Menthe fraîche', hex: '#98FB98', hsl: [120, 93, 79], emoji: '🌱' },
      { name: 'Citron vibrant', hex: '#FFFF00', hsl: [60, 100, 50], emoji: '☀️' },
      { name: 'Rose pétale', hex: '#FFB6C1', hsl: [350, 100, 84], emoji: '🌸' },
      { name: 'Bleu ciel', hex: '#87CEEB', hsl: [197, 71, 73], emoji: '🌤️' }
    ],
    'calme-automne': [
      { name: 'Rouge brique', hex: '#CD5C5C', hsl: [0, 47, 58], emoji: '🍂' },
      { name: 'Orange brûlé', hex: '#CD853F', hsl: [25, 76, 48], emoji: '🎃' },
      { name: 'Beige sable', hex: '#F4A460', hsl: [30, 87, 67], emoji: '🏜️' },
      { name: 'Marron noisette', hex: '#8B4513', hsl: [25, 75, 31], emoji: '🌰' }
    ],
    'ludique-été': [
      { name: 'Jaune tournesol', hex: '#FFFF00', hsl: [60, 100, 50], emoji: '🌻' },
      { name: 'Corail tropical', hex: '#FF7F50', hsl: [16, 100, 66], emoji: '🏝️' },
      { name: 'Turquoise lagons', hex: '#40E0D0', hsl: [174, 100, 56], emoji: '🏊' },
      { name: 'Violet lavande', hex: '#E6E6FA', hsl: [240, 67, 94], emoji: '🌸' }
    ],
    'magique-hiver': [
      { name: 'Bleu glacial', hex: '#4682B4', hsl: [207, 44, 49], emoji: '🧊' },
      { name: 'Argent lunaire', hex: '#C0C0C0', hsl: [0, 0, 75], emoji: '🌙' },
      { name: 'Pourpre royal', hex: '#9370DB', hsl: [275, 76, 64], emoji: '👑' },
      { name: 'Or stellaire', hex: '#FFD700', hsl: [45, 100, 59], emoji: '⭐' }
    ]
  };

  const generatePalette = useCallback((mood, season, variation = 0) => {
    const key = `${mood}-${season}`;
    let basePalette = paletteDatabase[key];

    if (!basePalette) {
      // Palette par défaut si combinaison non trouvée
      basePalette = paletteDatabase['calme-automne'];
    }

    // Appliquer variation si demandée
    if (variation > 0) {
      basePalette = basePalette.map(color => ({
        ...color,
        hsl: [
          (color.hsl[0] + variation * 30) % 360, // Hue shift
          Math.min(100, color.hsl[1] + variation * 10), // Saturation boost
          Math.min(100, color.hsl[2] + variation * 5)  // Lightness boost
        ]
      }));
    }

    setCurrentPalette(basePalette);
    return basePalette;
  }, []);

  const randomizePalette = useCallback(() => {
    const moods = ['énergique', 'calme', 'ludique', 'magique'];
    const seasons = ['printemps', 'été', 'automne', 'hiver'];

    const randomMood = moods[Math.floor(Math.random() * moods.length)];
    const randomSeason = seasons[Math.floor(Math.random() * seasons.length)];
    const randomVariation = Math.floor(Math.random() * 3); // 0, 1, ou 2

    return generatePalette(randomMood, randomSeason, randomVariation);
  }, [generatePalette]);

  return {
    currentPalette,
    generatePalette,
    randomizePalette
  };
};
```

### **Composant d'inspiration dynamique**
```javascript
import { useState, useEffect } from 'react';
import { motion, AnimatePresence } from 'framer-motion';

const InspirationWall = ({ mood, season }) => {
  const [inspirations, setInspirations] = useState([]);
  const [currentIndex, setCurrentIndex] = useState(0);

  // Base d'inspirations par mood/saison
  const inspirationDatabase = {
    'énergique-printemps': [
      { text: 'Pousses vertes qui percent le sol', emoji: '🌱', color: '#98FB98' },
      { text: 'Premiers rayons de soleil', emoji: '☀️', color: '#FFFF00' },
      { text: 'Fleurs qui s'ouvrent', emoji: '🌸', color: '#FFB6C1' },
      { text: 'Eau qui coule', emoji: '💧', color: '#87CEEB' }
    ],
    'calme-automne': [
      { text: 'Feuilles qui tombent doucement', emoji: '🍂', color: '#CD5C5C' },
      { text: 'Chaleur des sous-bois', emoji: '🌰', color: '#CD853F' },
      { text: 'Sable chaud du désert', emoji: '🏜️', color: '#F4A460' },
      { text: 'Terre humide après la pluie', emoji: '🌧️', color: '#8B4513' }
    ]
  };

  useEffect(() => {
    const key = `${mood}-${season}`;
    const inspirationSet = inspirationDatabase[key] || inspirationDatabase['calme-automne'];

    setInspirations(inspirationSet);

    // Changer d'inspiration toutes les 8 secondes
    const interval = setInterval(() => {
      setCurrentIndex(prev => (prev + 1) % inspirationSet.length);
    }, 8000);

    return () => clearInterval(interval);
  }, [mood, season]);

  return (
    <div className="inspiration-wall">
      <AnimatePresence mode="wait">
        <motion.div
          key={currentIndex}
          className="inspiration-item"
          initial={{ opacity: 0, y: 20, scale: 0.9 }}
          animate={{ opacity: 1, y: 0, scale: 1 }}
          exit={{ opacity: 0, y: -20, scale: 0.9 }}
          transition={{
            duration: 0.6,
            ease: [0.68, -0.55, 0.265, 1.55]
          }}
        >
          <div className="inspiration-content">
            <span className="inspiration-emoji" style={{ color: inspirations[currentIndex]?.color }}>
              {inspirations[currentIndex]?.emoji}
            </span>
            <p className="inspiration-text">
              {inspirations[currentIndex]?.text}
            </p>
          </div>

          <div className="inspiration-particles">
            {[...Array(5)].map((_, i) => (
              <motion.div
                key={i}
                className="particle"
                style={{
                  backgroundColor: inspirations[currentIndex]?.color,
                  left: `${20 + i * 15}%`
                }}
                animate={{
                  y: [-20, -60, -20],
                  opacity: [0, 1, 0]
                }}
                transition={{
                  duration: 3,
                  delay: i * 0.5,
                  repeat: Infinity,
                  ease: "easeInOut"
                }}
              />
            ))}
          </div>
        </motion.div>
      </AnimatePresence>
    </div>
  );
};
```

## 12. Tests & Validation Créatifs

### **Tests des composants créatifs**
```javascript
// __tests__/components/CreativeCanvas.test.tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { CreativeCanvas } from '@/components/CreativeCanvas';

describe('CreativeCanvas', () => {
  it('initialise correctement le canvas', () => {
    render(<CreativeCanvas width={400} height={300} />);

    const canvas = screen.getByRole('img', { hidden: true });
    expect(canvas).toBeInTheDocument();
  });

  it('change la couleur du pinceau', () => {
    render(<CreativeCanvas />);

    const brushButton = screen.getByRole('button', { name: /🖌️/ });
    fireEvent.click(brushButton);

    // Vérifier que la couleur a changé (difficile à tester directement avec Fabric.js)
    // On pourrait mocker Fabric.js pour ces tests
  });

  it('sauvegarde automatiquement', async () => {
    const mockOnSave = jest.fn();
    render(<CreativeCanvas onSave={mockOnSave} />);

    // Attendre 35 secondes (interval + délai)
    await waitFor(
      () => {
        expect(mockOnSave).toHaveBeenCalled();
      },
      { timeout: 35000 }
    );
  });
});
```

### **Tests des hooks créatifs**
```javascript
// __tests__/hooks/useCreativePalette.test.ts
import { renderHook, act } from '@testing-library/react';
import { useCreativePalette } from '@/hooks/useCreativePalette';

describe('useCreativePalette', () => {
  it('génère une palette pour un mood et une saison', () => {
    const { result } = renderHook(() => useCreativePalette());

    act(() => {
      result.current.generatePalette('calme', 'automne');
    });

    expect(result.current.currentPalette).toHaveLength(4);
    expect(result.current.currentPalette[0]).toHaveProperty('name');
    expect(result.current.currentPalette[0]).toHaveProperty('hex');
    expect(result.current.currentPalette[0]).toHaveProperty('emoji');
  });

  it('randomise une palette', () => {
    const { result } = renderHook(() => useCreativePalette());

    act(() => {
      result.current.randomizePalette();
    });

    expect(result.current.currentPalette).toHaveLength(4);
  });

  it('applique des variations à la palette', () => {
    const { result } = renderHook(() => useCreativePalette());

    act(() => {
      result.current.generatePalette('énergique', 'printemps', 1);
    });

    // La palette devrait être légèrement différente de la base
    expect(result.current.currentPalette).toHaveLength(4);
  });
});
```

Ce fichier MD contient toutes les informations nécessaires pour recréer un atelier créatif complet avec canvas interactif, génération de palettes, animations organiques et interface inspirante.
